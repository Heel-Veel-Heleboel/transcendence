import { AbstractEngine, Scene } from '@babylonjs/core';

/* v8 ignore start */
export function renderLoop(engine: AbstractEngine, scene: Scene) {
  // engine.runRenderLoop(() => {
  //   scene.render();
  // });
}
/* v8 ignore stop */
